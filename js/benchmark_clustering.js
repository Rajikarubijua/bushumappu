// Generated by CoffeeScript 1.6.2
(function() {
  var clusters_n;

  clusters_n = 237;

  define(["prepare_data"], function(prepare) {
    var clustering, raf;

    console.log("start");
    figue.KMEANS_MAX_ITERATIONS = 1;
    clustering = function(initial_vectors, stations) {
      console.time("clustering");
      window.clusters = prepare.setupClusterAssignment(stations, initial_vectors, clusters_n);
      console.timeEnd("clustering");
      return console.log(clusters.length);
    };
    raf = function(initial_vectors, stations) {
      return setTimeout((function() {
        clustering(initial_vectors, stations);
        return raf(initial_vectors, stations);
      }), 200);
    };
    return d3.json("data/benchmark_initialvectors.json", function(initial_vectors) {
      console.log("loaded 50%");
      return d3.json("data/benchmark_stations.json", function(stations) {
        console.log("loaded 100%");
        return raf(initial_vectors, stations);
      });
    });
  });

}).call(this);

/*
//@ sourceMappingURL=benchmark_clustering.map
*/
