// Generated by CoffeeScript 1.6.2
(function() {
  define(['routing', 'test_graph'], function(routing, testGraph) {
    var tests;

    tests = {
      benchOptimize: function() {
        var graph10, graph100, layout, mean, time, times;

        graph10 = testGraph.graph10, graph100 = testGraph.graph100;
        layout = new routing.MetroMapLayout({
          graph: graph10
        });
        times = (function() {
          var _i, _results;

          _results = [];
          for (_i = 1; _i <= 20; _i++) {
            time = +Date.now();
            layout.optimize();
            _results.push(time = +Date.now() - time);
          }
          return _results;
        })();
        mean = d3.mean(times);
        console.info("optimize", mean, "" + times);
        layout = new routing.MetroMapLayout({
          graph: graph100
        });
        times = (function() {
          var _i, _results;

          _results = [];
          for (_i = 1; _i <= 20; _i++) {
            time = +Date.now();
            layout.optimize();
            _results.push(time = +Date.now() - time);
          }
          return _results;
        })();
        mean = d3.mean(times);
        console.info("optimize", mean, "" + times);
        layout = new routing.MetroMapLayout({
          graph: graph100
        });
        times = (function() {
          var _i, _results;

          _results = [];
          for (_i = 1; _i <= 20; _i++) {
            time = +Date.now();
            layout.optimize();
            _results.push(time = +Date.now() - time);
          }
          return _results;
        })();
        mean = d3.mean(times);
        return console.info("optimize", mean, "" + times);
      }
    };
    return {
      tests: tests
    };
  });

}).call(this);

/*
//@ sourceMappingURL=test_bench.map
*/
